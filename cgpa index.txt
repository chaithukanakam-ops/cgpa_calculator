<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CGPA Calculator</title>
<style>
  body { font-family: Arial, sans-serif; padding: 20px; }
  input { margin: 5px; padding: 5px; width: 100px; }
  button { padding: 10px 20px; margin: 10px 0; }
  h1 { color: #2c3e50; }
  h2 { color: #16a085; }
</style>
</head>
<body>
<h1>Simple CGPA Calculator</h1>

<p>Enter each subject's <strong>Credits</strong> and <strong>Score/Grade Points</strong>:</p>

<div id="subjectsContainer">
  <div>
    Credits: <input type="number" step="0.5" class="credit">
    Score: <input type="number" step="0.1" class="score">
  </div>
</div>

<button onclick="addRow()">Add More Subjects</button>
<button onclick="calculateCGPA()">Calculate CGPA</button>

<h2 id="result"></h2>

<script>
function addRow(){
    const container = document.getElementById('subjectsContainer');
    const div = document.createElement('div');
    div.innerHTML = `Credits: <input type="number" step="0.5" class="credit"> Score: <input type="number" step="0.1" class="score">`;
    container.appendChild(div);
}

function calculateCGPA(){
    const credits = document.getElementsByClassName('credit');
    const scores = document.getElementsByClassName('score');
    let totalCredits = 0, totalPoints = 0;

    for(let i=0; i<credits.length; i++){
        const c = parseFloat(credits[i].value);
        const s = parseFloat(scores[i].value);
        if(!isNaN(c) && !isNaN(s)){
            totalCredits += c;
            totalPoints += c * s;
        }
    }

    if(totalCredits > 0){
        const cgpa = (totalPoints / totalCredits).toFixed(2);
        document.getElementById('result').innerText = `Your CGPA is: ${cgpa} / 10`;
    } else {
        document.getElementById('result').innerText = `Please enter valid credits and scores.`;
    }
}

// Initialize with one row
addRow();
</script>
</body>
</html>